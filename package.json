{
  "name": "solar-eclipse",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --base=/threejs-eclipse/",
    "preview": "vite preview",
    "publish": "git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "@types/three": "^0.158.2",
    "typescript": "^5.2.2",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "three": "^0.158.0"
  }
}
